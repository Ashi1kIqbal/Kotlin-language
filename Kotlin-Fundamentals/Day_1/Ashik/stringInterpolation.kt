fun main(){
    val myStr = "Hello World"
    var firstCharInstr = myStr[0]
    var lastCharInStr =myStr[myStr.length-1]
    var myLength = myStr.length
    println("First Character: $firstCharInstr and Last Character: $lastCharInStr and the length of myStr is: ${myStr.length}")
    println("First Character: $lastCharInStr")

}